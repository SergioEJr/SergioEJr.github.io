% Product rule as rectangle areas. See _template.tex / _preamble.tex for the
% shared setup; this file keeps only the driver switch and the drawing.
\ifnum\pdfoutput>0
  \documentclass[tikz,border=3pt]{standalone}         % pdflatex → PDF proof
\else
  \documentclass[tikz,border=3pt,dvisvgm]{standalone} % latex   → web SVG
\fi
\input{_preamble}

\begin{document}
\begin{tikzpicture}[x=1mm, y=1mm]
  \def\F{62}\def\G{40}\def\df{16}\def\dg{14}

  \draw[edge, ink]              (0,0)      rectangle (\F,\G);
   \draw[hair, muted]            (\F,\G)    rectangle (\F+\df,\G+\dg);
  \draw[edge, accent]           (0,\G)     rectangle (\F,\G+\dg);
  \draw[edge, figred]           (\F,0)     rectangle (\F+\df,\G);
 

  \node[left,  text=accent] at (-2,\G+\dg/2)     {$dg$};
  \node[left,  text=ink]    at (-2,\G/2)         {$g$};
  \node[below, text=ink]    at (\F/2,-2)         {$f$};
  \node[below, text=figred] at (\F+\df/2,-2)     {$df$};

  \node[text=accent]                    at (\F/2,\G+\dg/2)   {$f\,dg$};
  \node[text=figred, rotate=-90]        at (\F+\df/2,\G/2)   {$g\,df$};
  \node[text=muted, scale=0.8]        at (\F+\df/2,\G+\dg/2) {$O(d^2)$};
\end{tikzpicture}
\end{document}
